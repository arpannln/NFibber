//initialize global array to memoize our solutions
var Fibholder = [0, 1];

//building bottom up so we minimize # of operations
var findNthFib = (n) => {
  //return if we have already solved it
  if (Fibholder[n]) return Fibholder[n];

  //build solutions for what we don't have yet
  
};
